"error empty" = "输入不能为空"
"select help" = "{{ \"使用箭头选择: \" | faint }} {{ .NextKey | faint }} {{ .PrevKey | faint }} {{ .PageDownKey | faint }} {{ .PageUpKey | faint }} {{ if .Search }} {{ \"and\" | faint }} {{ .SearchKey | faint }} {{ \"toggles search\" | faint }}{{ end }}"
